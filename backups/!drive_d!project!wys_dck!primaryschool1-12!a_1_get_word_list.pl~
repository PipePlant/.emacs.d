use strict;
use warnings;
use Data::Dumper;
use Digest::MD5  qw(md5 md5_hex);
use Encode;
use JSON;
use feature qw(say);
use Win32::OLE;
use Win32::OLE::Enum;

my $word_file = "./1.docx";
my $document = Win32::OLE -> GetObject($word_file);
