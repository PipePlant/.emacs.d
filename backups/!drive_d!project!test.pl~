use warnings;
use strict;
use Data::Dumper;
use feature qw(say);
use JSON;
use List::Util qw(shuffle);
use Encode;
use Digest::MD5 qw(md5 md5_hex md5_base64);

for (my $i=1; $i <1000; $i++) { 
	system "cls"; 
	print $i; 
} 